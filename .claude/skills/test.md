---
description: Run the test suite
command: /test
---

Run the full test suite with verbose output:

```bash
uv run pytest tests/ -v
```

If any tests fail, investigate the failure and suggest a fix.
